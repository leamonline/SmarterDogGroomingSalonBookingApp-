services:
  app:
    build: .
    ports:
      - "3001:3001"
    environment:
      - NODE_ENV=production
      - JWT_SECRET=${JWT_SECRET:?JWT_SECRET is required}
      - PORT=3001
    volumes:
      - petspa-data:/app/data
    restart: unless-stopped

volumes:
  petspa-data:
